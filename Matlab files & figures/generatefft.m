% These are the command window lines that I called to 
% generate the Fourier transforms of my data
%% Normal Flight
plotfreq(Normal2) % rostral contralateral wing 
plotfreq(Normal3) % caudal contralateral wing
plotfreq(Normal4) % rostral ipsilateral wing
plotfreq(Normal5) % caudal ipsilateral wing

%% pitched flight 
plotfreq(Pitch2) % rostral contralateral wing 
plotfreq(Pitch3) % caudal contralateral wing
plotfreq(Pitch4) % rostral ipsilateral wing
plotfreq(Pitch5) % caudal ipsilateral wing

%% Rolled Flight
plotfreq(Roll2) % rostral contralateral wing 
plotfreq(Roll3) % caudal contralateral wing 
plotfreq(Roll4) % rostral ipsilateral wing 
plotfreq(Roll5) % caudal ipsilateral wing

